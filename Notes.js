//! step1
//* setup server 
//create server folder 
//install package.json $npm init -y (if not doing -y you can configure it on terminal)
//install more packages which we'll use later
//? npm i bcryptjs cookie-parser cors dotenv i cloudinary express jsonwebtoken mongoose multer nodemon 
//create server.js as main file and also set it as entry point
//create .gitignore file and add files that you don't want to push to github

//* create database